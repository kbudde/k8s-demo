#@ load("@ytt:data", "data")
#@ traefikLB = "lb." + data.values.shared.domain
#@ loadbalancerid = "df2268f6-26b6-44c0-a62f-475855ce33c6"
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: #@ traefikLB
    loadbalancer.openstack.org/enable-health-monitor: "true"
    loadbalancer.openstack.org/keep-floatingip: "true"
    loadbalancer.openstack.org/load-balancer-id: #@ loadbalancerid
    loadbalancer.openstack.org/proxy-protocol: v2
  name: traefik
  namespace: ingress
spec:
  externalTrafficPolicy: Local
  ports:
    - name: web
      port: 80
      protocol: TCP
      targetPort: web
    - name: websecure
      port: 443
      protocol: TCP
      targetPort: websecure
  selector:
    app.kubernetes.io/instance: traefik-ingress
    app.kubernetes.io/name: traefik
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: #@ traefikLB
    loadbalancer.openstack.org/enable-health-monitor: "true"
    loadbalancer.openstack.org/keep-floatingip: "true"
    loadbalancer.openstack.org/load-balancer-id: #@ loadbalancerid
    #! proxy protocol is not supported
    #! loadbalancer.openstack.org/proxy-protocol: v2
  name: traefik-udp
  namespace: ingress
spec:
  externalTrafficPolicy: Local
  ports:
    - name: websecure-http3
      port: 443
      protocol: UDP
      targetPort: websecure-http3
  selector:
    app.kubernetes.io/instance: traefik-ingress
    app.kubernetes.io/name: traefik
  type: LoadBalancer
