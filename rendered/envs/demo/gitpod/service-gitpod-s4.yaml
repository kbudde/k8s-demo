apiVersion: v1
kind: Service
metadata:
  annotations:
    a8r.io/repository: https://github.com/kbudde/k8s-demo
  name: gitpod-s4
spec:
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: gitpod-s4
